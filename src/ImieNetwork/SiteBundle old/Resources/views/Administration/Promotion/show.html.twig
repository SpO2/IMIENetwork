 {% extends "ImieNetworkSiteBundle:Administration:layout.html.twig" %} 

{% block content %}
    <h1>{{ entity.libelle }}</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entity.libelle }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ entity.annee }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
        </tbody>
    </table>
    {{ form(delete_form) }}

        <a href="{{ path('promotion') }}">
            <button class="btn btn-default">Retour à la liste des promotions</button>
        </a>

        <a href="{{ path('promotion_delete', { 'id': entity.id }) }}">
            <button class="btn btn-primary">Editer</button>
        </a>

{% endblock %}

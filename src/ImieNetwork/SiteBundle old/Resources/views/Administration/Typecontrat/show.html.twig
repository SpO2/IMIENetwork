 {% extends "ImieNetworkSiteBundle:Administration:layout.html.twig" %} 

{% block content %}
    <h1>{{ entity.libelle }}</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entity.libelle }}</td>
            </tr>
        </tbody>
    </table>
    
    {{ form(delete_form) }}
    
        <a href="{{ path('typecontrat') }}">
            <button class="btn btn-default">Retour à la liste des types de contrats</button>
        </a>

        <a href="{{ path('typecontrat_edit', { 'id': entity.id }) }}">
            <button class="btn btn-primary">Editer</button>
        </a>

{% endblock %}

{% extends "ImieNetworkSiteBundle:Administration:layout.html.twig" %} 

{% block content %}
    <h1>Modifiier cet utilisateur</h1>

    {{ form(edit_form) }}

{{ form(delete_form) }}
    
            <a href="{{ path('utilisateur') }}">
          <button class="btn btn-default">Retour à la liste des utilisateurs</button>
        </a>

{% endblock %}

 {% extends "ImieNetworkSiteBundle:Eleve:layout.html.twig" %} 

{% block content %}
<div id="my_carousel" class="carousel slide" data-ride="carousel">
    <!-- Bulles -->
    <ol class="carousel-indicators">
        <li data-target="#my_carousel" data-slide-to="0" class="active"></li>
                {% for evenement in evenements %}
        <li data-target="#my_carousel" data-slide-to="1"></li>
       {%endfor%}
    </ol>
    <!-- Slides -->
    <div class="carousel-inner">
        <!-- Page 1 -->
        <div class="item active">  
            <div class="carousel-page">
                <p>Bienvenu sur le site de l'IMIE</p>
                <p>L’INSTITUT DE LA FILIÈRE NUMÉRIQUE</p>
                <p>Créée en 1994, l’école de la filière numérique est actuellement 
                    implantée sur Nantes, Angers et Rennes. Pour intégrer le monde du 
                    travail numérique dans les meilleures conditions, IMIE vous propose
                    d’obtenir des diplômes reconnus, de maîtriser les technologies les plus
                    récentes et perfectionner votre pratique de la langue anglaise.</p>
            </div> 
            <div class="carousel-caption">Profitez de nos fonctionnalités</div>
        </div>   


        {% for evenement in evenements %}
        <!-- Page 3 -->
        <div class="item">  
            <div class="carousel-page">
                <p>{{ evenement.libelle }}</p>
                <p>{{ evenement.description }}</p>
            </div>  
            <div class="carousel-caption"></div>
        </div>
       {%endfor%}

    </div>
    <!-- Contrôles -->
    <a class="left carousel-control" href="#my_carousel" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left"></span>
    </a>
    <a class="right carousel-control" href="#my_carousel" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right"></span>
    </a>
</div>

<div class="media">
    {% for enquete in enquetes %}
    <div class="media-body">
        <h4 class="media-heading">Enquête !</h4>
        <p>{{ enquete.contenu }}</p>
        <p> <a href="{{ enquete.url }}">{{ enquete.url }}</a> </p>
    </div>
    {%endfor%}
</div>

<style>
    .carousel-page
    {   
        width:100%;
        height:250px;
        background-color:#1BAFCD;
        color:white;
        text-align: center;
    }
</style>
     {% endblock %}
     
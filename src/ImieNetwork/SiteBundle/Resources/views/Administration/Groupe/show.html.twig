 {% extends "ImieNetworkSiteBundle:Administration:layout.html.twig" %} 

{% block content -%}
    <h1>Groupe</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entity.libelle }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('administrationgroupeIndex') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('administrationgroupeedit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

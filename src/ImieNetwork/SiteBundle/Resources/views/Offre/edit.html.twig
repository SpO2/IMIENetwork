{% extends "ImieNetworkSiteBundle:Eleve:layout.html.twig" %}

{% block content %}
    <h1>Offre edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('offre') }}">
            Back to the list
        </a>
    </li>
    {#{{ app.security.getToken().getUser().getUsername() }}#}
    {#% if is_granted('ROLE_USER') %#}
        <li>{{ form(delete_form) }}</li>
    {#% endif %#}
    
</ul>
{% endblock %}
